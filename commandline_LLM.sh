#!/bin/bash

jq -r 'select(.works) | {id: .id, title: .title, total_works_count: (.works | map(.books_count | tonumber) | add)}' series.json | jq -s 'sort_by(-.total_works_count) | .[:5]'














































